record_refill:
  name: Record Refill
  description: Manually record a heating oil refill
  fields:
    volume:
      name: Volume
      description: Volume added in liters (optional)
      example: 1000
    entry_id:
      name: Config Entry ID
      description: Target a specific tank config entry (optional, defaults to all tanks)
      example: abc123
