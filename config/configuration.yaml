# Loads default set of integrations
default_config:

# Enable the frontend
frontend:

# Enable logging
logger:
  default: info
  logs:
    custom_components.heating_oil_monitor: debug
    homeassistant.loader: debug

# Simplified config - remove problematic settings
homeassistant:
  # Don't need allowlist for development

# Remove TTS for now to avoid missing dependencies
# tts:
#   - platform: google_translate

automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml